name: 'Auto Assign'
on:
  pull_request:
    types:
      - opened
      - reopened
      - ready_for_review
      - synchronize
      - assigned

jobs:
  add-reviews:
    runs-on: ubuntu-latest
    steps:
      - uses: twoequal0/auto-assign-bot@v1.3.0
